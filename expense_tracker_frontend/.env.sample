# Feature flag: use local mock data by default (1 = mock, 0 = backend API)
REACT_APP_USE_MOCK=1

# Backend API URL (used when REACT_APP_USE_MOCK=0)
REACT_APP_API_URL=http://localhost:4000

# Optional: theme and feature flags retained
REACT_APP_FEATURE_FLAGS=
REACT_APP_THEME=OceanProfessional
